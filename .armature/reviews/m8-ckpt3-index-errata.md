# Review Verdict: m8-ckpt3-index-errata

**Reviewed:** Milestone 8, Checkpoint 3 ("Navigation, errata, deviations") — items 12-16 of `.taskmaster/docs/milestone-8-docs-remediation.md`, confirmed against the PRD text directly (item 14's ten named errata topics map 1:1 onto E1-E10 in declared order; item 16's five named deviation categories plus the two checkpoint-2 handoff items map onto the seven filed `DEV-*` entries).
**Changeset:** uncommitted working tree over HEAD `448f029` — modified `README.md`, `docs/compatibility_and_deviations.md`, `docs/interop_overview.md`, `spec/README.md`, `tests/test_doc_snippets.py`; new `docs/README.md`, `spec/Limnalis-v0.2.2-errata.md`. Confirmed via `git status --porcelain=v1` — exactly these 7 paths, nothing else dirty.
**Method:** hash-verified every vendored spec file plus `schemas/`, `fixtures/`, `src/` against HEAD; read every diff hunk in full; re-derived all ten errata entries from primary sources (both spec editions, all 3 vendored JSON schemas via `python3 -c "json.load(...)"`, the actual vendored fixture-corpus source text, the extension corpus, and the relevant `src/` code) rather than trusting the errata's own prose; execution-confirmed 3 of 7 deviation-ledger entries by running real `.lmn` snippets through `python -m limnalis normalize` and inspecting the CLI's actual error/output; read `compare.py`/`runner.py` in full and mapped every sentence of the rewritten "Extra-diagnostic handling" section to specific code lines; independently re-globbed `docs/**/*.md` and diffed against the new index; ran the full test suite and the doc-snippet suite to completion.

## Scope Compliance
- Declared scope: docs + tests only, matching the PRD's hard constraint ("No changes to `src/` except NONE").
- **Critical constraint verified by hash, not by diff alone:** `git hash-object` on all 7 vendored `spec/` files matches `git rev-parse HEAD:<path>` exactly for every file (`Limnalis-v0.2.1.pdf`, `Limnalis-v0.2.2-reconstructed.md`, `Limnalis-v0.2.2-reconstructed.pdf`, `Limnalis-v0.2.2-recovery-notes.md`, `Limnalis-v0.2.2.md`, `Limnalis_conformance_matrix_v0.2.1.xlsx`, `limnalis_conformance_matrix_v0.2.2.md`). `git diff --name-status HEAD -- schemas/ fixtures/ src/` is empty (exit 0, no output); no untracked files exist under those trees. **Byte-unchanged, confirmed.**
- Files modified/added match the stated changeset exactly (5 modified + 2 new = 7 paths, verified via `git status --porcelain=v1`).

## Item 1 — Errata (`spec/Limnalis-v0.2.2-errata.md`), all 10 entries re-verified from primary sources

| # | Topic (PRD item 14) | Verdict |
|---|---|---|
| E1 | `ClaimNode.eval?: EvalNode` phantom field | **CLEAN.** `Limnalis-v0.2.2.md:1343` has it; schema's `ClaimNode` (`$defs.ClaimNode`) has `additionalProperties:false` with exactly `{annotations,expr,id,kind,node,refs,semanticRequirements,usesAnchors}` and no `EvalNode` `$def` exists anywhere in the AST schema; reconstruction's `ClaimNode` (lines 1346-1354) also omits `eval`. |
| E2 | `TermSpecNode` orphan vs schema's `AnchorTermNode` | **CLEAN.** Grepped all three artifacts — `TermSpecNode` appears only at the one cited consolidated line (1347) and nowhere else. Schema's `AnchorTermNode` is the described 3-way `oneOf` (symbol/claim/expr). Both editions' §9.1 prose (`Limnalis-v0.2.2.md:885`, `reconstructed.md:652`) read verbatim `term: Term \| ClaimId` — no prose warrant for the `expr` variant anywhere. Reconstruction has no `AnchorNode:` block (only `anchors: [AnchorNode]` inside `BundleNode`, exactly at line 1302 as cited). |
| E3 | `BundleNode.frame` optionality | **CONCLUSION CORRECT, ONE CITED PIECE OF EVIDENCE IS WRONG — see Finding A below.** Schema: root schema's `required` includes `frame`; `frame` is `$ref: FrameOrPatternNode` = `oneOf[FrameNode, FramePatternNode]`. Reconstruction line 1292 (exact): `frame: FrameNode \| FramePatternNode`, no `?`. These two legs fully support the entry's conclusion. The third evidence leg is misattributed (Finding A). |
| E4 | Glyph-table conflicts (EMRG/UNDEF/NULL) | **CLEAN.** Consolidated `:1087` and reconstruction `:794-797` quoted verbatim and correctly — 3 of 4 differ, only PARA (⊥) agrees. Reconstruction's EBNF (`:1256`, `:1281`) uses its own glyphs; consolidated defines no grammar productions for its column. `grep` of `src/` and `grammar/limnalis.lark` for every glyph returns nothing — implementation only recognizes ASCII (`normalizer.py:1080`, `tokens.index("EMRG")`). |
| E5 | Lint rules 11 & 15 wording | **SUBSTANCE SOUND, ONE LINE CITATION WRONG — see Finding B below.** Rule 11 citation exact (`reconstructed.md:954`). Consolidated §9.4 (`:936-939`, "Exact-set matching rule... names exactly that set and the same task") independently confirms the reconstruction's rule-15 wording is the more precise restatement, and `builtins.py:1464-1469` implements exact-set equality (`needed_joint_set == ja_anchor_set`) — the adjudication is soundly argued from §9.4 + implementation as required. |
| E6 | `sourceAggregate` §10.3 vs §18.2 | **CLEAN.** `Limnalis-v0.2.2.md:1020` (`sourceAggregate: EvalNode`, no `?`) and `:2023` (§18.2, "optional sourceAggregate...") both exact. Conformance-result schema's `TransportExpectation.required == ["status"]` only. `runtime/models.py:258`: `srcAggregate: EvalNode \| None = None`. Confirmed practically unimplementable for `metadata_only`: `builtins.py:2208-2217` constructs `metadata_only` results with `srcAggregate=None`. |
| E7 | Rule 21 `note(…)` vs grammar | **CLEAN.** Both editions' rule 21 (`:1114`, `:964`) quoted exactly. Reconstruction EBNF `:1263`: `NoteExpr ::= "note" String ;` — no parens. Vendored corpus B1 source (read directly from `fixtures/limnalis_fixture_corpus_v0.2.2.yaml`) literally contains `c5: note "N-1 is acceptable...";` — bare form. `normalizer.py:1275` dispatches on bare `words[0] == "note"`; `_parse_note` (`:1000`) takes everything after the keyword as the string, no paren handling. |
| E8 | B1 dimensioned literal vs both grammars | **CLEAN.** Read the actual corpus B1 source text directly — it contains `demand_ramp_gt(0.02_pu_per_min)`, confirming the literal is real, not hypothetical. Both editions' `Number`/`Ident` productions (consolidated `:1188`/`:1187`; reconstruction `:1013-1014`, exact) admit no digit-led, unit-suffixed token. `normalizer.py:152` `_NUMBER_RE = r"^-?\d+(?:\.\d+)?$"` does not match; falls through to `SymbolTermNode` at `:1609`, execution-plausible and matching the errata's "normalized as a symbol term" claim. |
| E9 | A11 narrative vs vendored corpus, D5 realization | **CLEAN.** Consolidated `:1900+` (exact — "A11." literally starts at line 1900) quoted correctly including the `by_context_v1` fixture and the 10/nominal-t1, 20/stress-t2 values. Parsed the actual vendored corpus A11 case: single session `s1`, fixtures `test://eval/baseline_v1` + `test://baseline/const10` (+ an unmentioned-but-not-contradicting `series_9_10_11`); `by_context_v1` is absent, confirmed by direct string search. Parsed extension corpus D5: name "Shared vs isolated session baseline state (spec A11 narrative)" (exact), fixture `test://baseline/by_context_v1` (exact), sessions `s_shared`/`s_isolated` (exact) — the D5 cross-reference is verbatim accurate. |
| E10 | Reconstruction's degrade rule omits precondition gate | **CLEAN.** Reconstruction `:537-546` (exact range) states only "if relevant detail is lost" as the degrade trigger — no precondition mention, in contrast to its own `preserve` bullet three lines above which does list preconditions. Consolidated `:1002-1004` (exact) explicitly dual-gates: "if preconditions hold and semantic_requirements ∩lose = ∅, preserve; otherwise...". `builtins.py:_execute_degrade` (`:2464-2511`) implements the dual gate precisely: precondition failure short-circuits to `N[transport_precondition]`/`status="blocked"` *before* the truth-degradation table ever runs; only requirement-loss reaches `_degrade_truth`. |

### Finding A (errata correction required) — `spec/Limnalis-v0.2.2-errata.md:26`

The sentence "The consolidated edition's own body text (§ Abstract Machine Signature, `Limnalis-v0.2.2.md:1038`: `frame: FramePattern \| Frame`) contradicts its A.9" is **factually wrong on both the section name and the node identity**:
- `Limnalis-v0.2.2.md:1038` is inside `## 11. Baselines, Unbound Behavior, and Emergence`, as part of the **`Baseline`** node's own `frame` field (`Baseline.frame: FramePattern | Frame` — describing what frame a baseline is scoped to, a different node from `BundleNode`).
- The actual "Abstract Machine Signature" section is at `Limnalis-v0.2.2.md:97-132` (confirmed via `grep -n "^###"`), and its own relevant field — `StepContext.frame` at line 112 — is typed `FrameNode` only (required, no union), which doesn't even match the quoted text `FramePattern | Frame`.

This does not flip E3's conclusion — the schema and reconstruction legs of the same entry independently and correctly establish that `frame` is required and `FrameOrPatternNode`-typed — but as literally written, this citation attaches false evidence (wrong node, wrong section) to a true conclusion, which is exactly the failure mode the review brief called "the worst possible outcome." **Requires correction**: either drop this clause or replace it with a citation that actually supports the claim (e.g., `StepContext.frame: FrameNode`, required, at line 112 — noting it is `StepContext` not `BundleNode`, so it would need honest reframing, not a straight swap).

### Finding B (errata correction required) — `spec/Limnalis-v0.2.2-errata.md:40`

The reconstruction citation `(:956)` for rule 15's text is off by two lines. Counting from the reconstructed lint-rules list (item 2 at line 945), item 15 ("Required joint adequacy is checked by exact active-anchor set and task.") is actually at **line 958**; line 956 is item 13 ("Every active anchor declares an adequacy assessment for the current task."), an unrelated rule. **Requires correction**: `:956` → `:958`.

Both Finding A and Finding B are citation-precision defects in *supporting evidence*, not errors in the entries' *claims or correct-artifact adjudications* — every one of the 10 top-line conclusions I independently re-derived from primary sources matches what the errata states. But given the explicit bar set for this checkpoint, both are called out as required corrections rather than folded silently into "clean."

## Item 2 — Deviation ledger (7 entries) and rewritten "Extra-diagnostic handling" section

All 7 `DEV-*` entries verified; 3 execution-confirmed per the task's requirement, 4 verified at the citation/source level:

- **DEV-UNBOUND-REF** (execution-confirmed): built a real `.lmn` bundle using `|inf:kindx|` and separately `|∞:kindx|`, ran `python -m limnalis normalize` — both produce exactly `error: normalization error ... invalid baseline reference '|inf:kindx|'` / `'|∞:kindx|'`, matching the cited `normalizer.py:1575` and reconstruction `:1279-1280` (exact text confirmed).
- **DEV-TRUTHPOLICY-DEAD** (execution-confirmed via grep): `models/ast.py:501` is exactly `truthPolicy: str | None = None`; `normalizer.py:713,724` exactly map surface `truth_policy` → `truthPolicy`; `grep -rn "truthPolicy|truth_policy" src/limnalis/runtime/*.py` returns **zero matches** — confirmed dead at runtime. Cross-checked against `.armature/reviews/m8-ckpt2-contradictions.md`, which independently discovered this exact fact and explicitly flagged it as "candidate for the checkpoint-3 deviation ledger" — the entry fulfills that handoff.
- **DEV-INTERVENTION-CLAUSE** (execution-confirmed, exact match): reconstruction `:1249-1251` quoted exactly (`CausalOp` has no colon form; `InterventionClause` is a separate trailing production). `_CAUSAL_RE` (`normalizer.py:151`) confirms the implementation's bracket-embedded form. Built `overload(line_B) =>[do] stable(grid) intervention shed_load` and ran it through `python -m limnalis normalize` — the output's `rhs.name` is **literally** `"stable(grid) intervention shed_load"`, an exact character-for-character match to the ledger's claimed misparse output, with no parse error raised (confirming "misparses" rather than "errors" as claimed).
- **DEV-ADEQ-CONTESTED**: `builtins.py:1162-1196` confirmed to trigger `method_conflict` on differing `aa.method` URIs (not score divergence) exactly as described; ADR-008's amendment (`docs/adr/008-contested-adequacy-aggregation.md:46`) exists and is append-only. This entry also correctly folds in the "Additionally, its trigger mechanism differs" clause that ckpt2's review explicitly asked to be included alongside the aggregation-rule finding — confirmed present.
- **DEV-REASON-VOCAB**: `builtins.py:981-998` (N-path, `not_yet_applicable`) and `:1535` (`no_adequacy_result`) both exact. `no_adequacy_result` does not appear anywhere in either spec file; §8.5's full N-reason list (read in full) confirms its absence.
- **DEV-ASSUMPTION-SURFACE**: `models/ast.py:362` exact (`class AssumptionNode`); zero hits for "assumption" (case-insensitive) in `grammar/limnalis.lark` and in `normalizer.py`; schema has `AssumptionNode` in `$defs`.
- **DEV-EMRG-HYSTERESIS**: consolidated `:1056-1057` exact; `models/ast.py:280-281` exact (`hysteresis`/`witness` fields); `normalizer.py:1080-1122` (exact range) — the EMRG surface parser builds only `property`/`onset`/`persistsWhile`/`dissolvesWhen`, never reads a `hysteresis` or `witness` token; `builtins.py:734` walks `hysteresis` only inside a generic reference-collection `_walk` helper, not for its own semantics.

**Extra-diagnostic handling section** — mapped every sentence to current code:
- "Unmatched error/fatal actual diagnostics are reported as mismatches... regardless of how many expected entries matched": confirmed at `compare.py:608-626` — the check fires whenever `expected.get("diagnostics") is not None` (no "at least one matched" guard exists), and only `severity in {"error","fatal"}` extras become mismatches.
- "Exactly two documented exemptions": confirmed — claims reverse-check has the note-claim exemption (`compare.py:722-726`); transports reverse-check has the bridge-id exemption (`compare.py:786-790`); blocks reverse-check (`:752-770`) has **no** exemption, so the count of two is exact, not approximate.
- "Vendored B1 c5" / "vendored A7" exemption examples: confirmed B1's `c5` is the `note` claim found in Item 1/E7 above; confirmed A7's actual bridges are named `b_pattern` and `b_exec` (read directly from the corpus), matching the code comment at `compare.py:669-674` word-for-word.
- "Warnings never affect `passed`": confirmed — `_compare_eval_snapshot` (`:107-119`) appends to a separate `warnings` list; `CaseComparison.passed = len(mismatches) == 0` (`:644`) never consults `warnings`.
- "Two injected codes, self-fulfilling comparison": `runner.py:_build_injected_diagnostics` (`:648-664`) confirmed to inject **only** `frame_pattern_completed`/`logical_composition`, copying the expected diagnostic dict verbatim into the actual stream (`injected.append(dict(diag))`) — literally self-fulfilling as claimed.
- Cross-checked the whole rewrite against `src/limnalis/conformance/agents.md`'s existing "Behavioral Directives" (a pre-existing file, not part of this diff) — it independently states the same two-exemption rule, the same B1/A7 examples, and explicitly names `docs/compatibility_and_deviations.md` as where the injected-diagnostics limitation is documented. The rewrite fulfills a contract that already existed pointing at it.

**Verdict on Item 2: fully clean, no defects found.**

## Item 3 — `docs/README.md` index

- Ran `tests/test_doc_snippets.py` — 27 collected, all pass (`pytest --collect-only -q` reports 27; full run is all dots, zero F/E).
- Independently re-globbed `docs/**/*.md` (44 files including `README.md` itself) and checked each of the other 43 for inclusion in the index text via a from-scratch script (not the test's own code) — **0 missing**, matches the canary.
- Spot-checked 12 links (exceeding the required 10) by direct filesystem existence check: `docs/getting_started.md`, `docs/reading_limnalis.md`, `docs/cookbook/conformance_testing.md`, `docs/adr/008-contested-adequacy-aggregation.md`, `docs/jsonld_rdf_note.md`, `spec/README.md`, `examples`, `editor`, `schemas`, `fixtures`, `docs/writing_a_transport_handler.md`, `docs/limnalis_ast_pressure_points_settled_v0.2.2.md` — all resolve.
- Reading order (Getting Started → How to Read Limnalis → How Evaluation Works → Architecture) is a sane install→orient→semantics→architecture progression; verified `reading_limnalis.md` actually has the claimed `## For Architects` / `## For Plugin Authors` / `## For Governance Reviewers` structure (`grep -n "^##"` confirms all three headers).
- Historical docs: all 5 listed (`implementation_notes.md`, `milestone_3b_notes.md`, `milestone_3c_status.md`, `m6b_stress_bundles.md`, `release_candidate_status.md`) confirmed to carry a `> **Historical — superseded (2026-08-24).**` banner in their first lines (these banners predate this checkpoint's diff — added in checkpoint 2 per its own review record — and remain intact).
- ADR one-liners spot-checked (3 of 8): ADR-002 body ("13-Phase Step Runner with Primitive Operations") vs index's "Execution model" — accurate summary. ADR-004 body ("The public API is frozen at RC time under `limnalis.api.*`") vs index's "Public API freeze (`limnalis.api.*`)" — exact. ADR-006 body ("Evidence inference is implemented as an **opt-in extension**") vs index's "Evidence inference is opt-in" — exact.

**Verdict on Item 3: fully clean, no defects found.**

## Item 4 — Root README + spec/README edits

- Root `README.md` diff is a minimal, additive "## Documentation" section plus two tree-diagram lines (`examples/`, `editor/`) and one description-line update; both new directories confirmed to exist at repo root. All listed links spot-checked resolve (above).
- `spec/README.md` diff adds one table row (errata) and rewrites the "Version note" paragraph into a "Reader precedence" paragraph. The AST-shape precedence-inversion claim — "the seam audit found the consolidated edition's Appendix A.9 to be the least reliable description of the canonical AST... e.g. A.9's phantom `ClaimNode.eval` field, its orphan `TermSpecNode` type, and its optional `frame?: FrameNode`" — names exactly E1, E2, E3, all three independently re-verified true above (E3's conclusion, not its flawed third citation).

**Verdict on Item 4: fully clean.**

## Item 5 — `interop_overview.md` softening

- The rewritten "Rule of thumb" paragraph (`docs/interop_overview.md:49-54`) no longer claims an RDF pipeline, explicitly states "RDF/linked-data integration is **not implemented**," and points at `jsonld_rdf_note.md`. Confirmed consistent: `jsonld_rdf_note.md` opens with "Status: Exploratory. Not part of the Limnalis specification" and its "Potential Approach" section uses conditional language ("could carry an optional `@context`") throughout, never claiming an existing pipeline. `grep -rn "@context|jsonld|json-ld|JsonLd|JSONLD" src/limnalis/interop/*.py` returns nothing — the "not implemented" claim matches the actual code.
- **Advisory (non-blocking, pre-existing, outside the touched diff hunk):** `docs/interop_overview.md:46` ("Projections are useful for documentation, RDF interop, and toolchains that consume LinkML") still lists "RDF interop" as a LinkML-projection use case, five lines above the new hard "not implemented" language. This line was not touched by this checkpoint's diff. It is defensible in the abstract (LinkML's own ecosystem has RDF/OWL generators independent of anything Limnalis implements), but it sits close enough to the new "not implemented" sentence that a careful reader could read tension between them. Worth tightening on a future pass; does not misstate current capability on its own.

**Verdict on Item 5: clean, one pre-existing low-priority advisory noted.**

## Item 6 — Gates

- **Full suite:** `python -m pytest tests/ -q`, exit code 0. Counted output programmatically: **1121 passing dots, 0 F/E markers** — matches the expected count exactly.
- **Snippet gate:** `tests/test_doc_snippets.py --collect-only -q` reports **27** tests; full run of the file is all-pass.
- **Fences balanced:** counted `` ``` `` occurrences in all touched/new files — `docs/README.md` (0), `spec/Limnalis-v0.2.2-errata.md` (0), `README.md` (18), `docs/compatibility_and_deviations.md` (4), `docs/interop_overview.md` (12), `spec/README.md` (0) — all even.
- **Git status:** `git status --porcelain=v1` shows exactly the 7 stated paths (5 `M`, 2 `??`) — confirmed twice, at the start and end of this review session.

**Verdict on Item 6: fully clean.**

## Verdict: PASS_WITH_ADVISORIES

Every substantive claim in this checkpoint — all 10 errata conclusions, all 7 deviation-ledger entries (3 execution-reproduced with exact output matches), every sentence of the rewritten extra-diagnostic section, the docs/README.md index's completeness and link integrity, the README/spec/README edits, and the interop softening — independently re-derived true from primary sources (spec text, schema JSON, vendored corpus source, extension corpus, and running code), not merely re-read as asserted. All four gates are green. The critical byte-unchanged constraint holds by cryptographic hash, not just `git diff`.

Two citation-precision defects were found in the errata's supporting evidence (Findings A and B above) — a misattributed section/node in E3's third evidence leg, and an off-by-two line number in E5. Neither changes any entry's top-line claim or correct-artifact adjudication, which is why this is `PASS_WITH_ADVISORIES` rather than `FAIL` — but given the review brief's explicit standard ("an errata entry that is itself wrong would be the worst possible outcome"), false evidence attached to a true conclusion is a real defect in a document whose entire value proposition is precise, checkable citations, and is called out as a required correction rather than absorbed silently.

## Required Changes
- `spec/Limnalis-v0.2.2-errata.md:26` (E3): the "consolidated edition's own body text... contradicts its A.9" clause cites `Limnalis-v0.2.2.md:1038` under the label "§ Abstract Machine Signature" — that line is actually inside "## 11. Baselines, Unbound Behavior, and Emergence" and describes `Baseline.frame`, not `BundleNode.frame` or anything in the Abstract Machine Signature section (which is at lines 97-132 and doesn't state this union type). Remove this evidence clause or replace it with a citation that actually supports the claim.
- `spec/Limnalis-v0.2.2-errata.md:40` (E5): the reconstruction citation for rule 15's text should be `:958`, not `:956` (line 956 is rule 13, an unrelated rule).

Both are mechanical, single-line corrections; neither requires re-litigating any entry's conclusion, and neither touches a vendored (immutable) artifact.

## Advisories (non-blocking)
- `docs/interop_overview.md:46` retains "RDF interop" as a LinkML-projection use case, adjacent to but not touched by the new "not implemented" language 5 lines below. Consider tightening on a future pass.

## Rollback Recommendation: NO
No invariant violation, no scope violation, no vendored-artifact modification, and no wrong top-line conclusion found anywhere in the seven-file changeset after execution-based re-verification of every numbered review item. The two required changes are single-line citation corrections inside the new (not vendored) errata file and can be applied directly without another full implementer/reviewer cycle if the orchestrator prefers a fast-follow over a formal re-checkpoint.

## On a milestone-wide red team (reviewer's view, for the orchestrator's decision record)
Not warranted: this is a docs+tests-only milestone with `src/` byte-identical to HEAD across all three checkpoints (hash-verified, not just diffed), and each checkpoint already received an independent, execution-based reviewer pass — not implementer self-report — that re-derived claims from primary sources and code rather than trusting prose (this review reproduced 3 deviation claims by running the CLI and got exact-string-match output on the sharpest one). A red team's marginal value is in adversarial engineering-quality/architecture critique, which has little additional surface on a documentation deliverable with no runtime, security, or architectural change; the two real defects this review did find (both narrow citation errors, not conclusion errors) are exactly the class of thing this kind of hard-verification reviewer pass catches, and did catch, without needing an adversarial framing.
